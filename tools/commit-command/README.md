# Smart Commit

Slash command that generates conventional commit messages from your staged changes.

## Usage

Use `/smart-commit` to analyze staged changes and generate an appropriate commit message.

## Features

- Analyzes diff to determine commit type (feat, fix, refactor, etc.)
- Follows conventional commits specification
- Presents message for review before committing
